//Usuário digitará um número e
//o programa completará o número digitado até cem, 
//apenas com números pares.

usuario = 30
for(let i=usuario; i<=100;i++){
    if(i%2==0)
    //se o resto de i divisivel por 2 for igual a 0
        console.log(i)
}